import { createTheme } from "@mui/system";

export const theme = createTheme({
    palette: {
        primary: {
            white: '#FFFFFF'
        }
    },
    typography: {
        alata: 'Alata',
        heading: {
            fontFamily: 'Josefin Sans',
        },
    }
});

